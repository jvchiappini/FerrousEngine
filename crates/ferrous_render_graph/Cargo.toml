[package]
name = "ferrous_render_graph"
version = "0.1.0"
edition = "2021"
description = "RenderPass trait and FramePacket for FerrousEngine — used by the renderer and by external pass implementors"

[dependencies]
wgpu    = "23.0"
glam    = "0.29"
ferrous_core = { path = "../ferrous_core" }

[package]
name = "ferrous_ui_core"
version = "0.1.0"
edition = "2021"

[dependencies]
slotmap = "1.0"
glam = { workspace = true }
serde = { version = "1.0", features = ["derive"] }
ferrous_assets = { path = "../ferrous_assets", optional = true }

[features]
assets = ["dep:ferrous_assets"]

[package]
name = "ferrous_gpu"
version = "0.1.0"
edition = "2021"

[dependencies]
wgpu = { workspace = true }
anyhow    = "1.0"
thiserror = "1.0"
log = "0.4"

[target.'cfg(target_arch = "wasm32")'.dependencies]
web-sys = { version = "0.3.94", features = ["console"] }
wasm-bindgen = "0.2"

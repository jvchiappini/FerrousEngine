fn main() {
    // Este fichero era un prototipo antiguo; ahora tenemos el ejemplo más
    // completo dentro del crate `ferrous_renderer`. Lo eliminamos para evitar
    // ambigüedades.
    println!("Este archivo ya no se usa. Consulte `crates/ferrous_renderer/examples/simple_triangle.rs`.");
}

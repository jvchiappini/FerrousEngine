Place a TrueType font file (.ttf) in this directory.

For example, you could copy `Roboto-Regular.ttf` or any other open font.
The editor will attempt to load `assets/fonts/Roboto-Regular.ttf` on startup.
